# Purpose

The work from now on is focusing on building a clean modern Angular `intl-tel-input` package.
The package will be built using the latest Angular version and will be published on npm as `@angular-intl-tel-input`
